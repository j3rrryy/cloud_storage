apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cloud-storage.auth.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: auth
    {{- include "cloud-storage.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.auth.replicaCount }}
  selector:
    matchLabels:
      app: auth
  template:
    metadata:
      labels:
        app: auth
    spec:
      containers:
      - name: auth
        image: "{{ .Values.global.image.repository }}:{{ .Values.auth.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - containerPort: {{ .Values.auth.service.grpcPort }}
          name: grpc
        - containerPort: {{ .Values.auth.service.httpPort }}
          name: http
        envFrom:
        - configMapRef:
            name: {{ include "cloud-storage.auth.fullname" . }}-config
        {{- if .Values.secrets.auth }}
        - secretRef:
            name: {{ include "cloud-storage.auth.fullname" . }}-secret
        {{- end }}
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-auth-postgres
              key: password
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-auth-redis
              key: redis-password
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          {{- toYaml .Values.auth.resources | nindent 10 }}
