apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-storage.mail.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: mail
    {{- include "cloud-storage.labels" . | nindent 4 }}
spec:
  selector:
    app: mail
  ports:
  - name: http
    port: {{ .Values.mail.service.httpPort }}
    targetPort: http
  type: ClusterIP
