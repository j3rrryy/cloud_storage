apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cloud-storage.mail.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: mail
    {{- include "cloud-storage.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.mail.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
  KAFKA_SERVICE: {{ include "cloud-storage.kafka.fullname" . }}:9092
